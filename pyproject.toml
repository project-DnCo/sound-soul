[project]
name = "sound-soul"
version = "0.1.0"
description = "Soul Eater themed discord bot."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []


[project.scripts]
sound_soul = "sound_soul:main"


[tool.uv]
cache-dir = "./.uv_cache"


[build-system]
requires = ["uv_build >= 0.9.5, <0.10.0"]
build-backend = "uv_build"
